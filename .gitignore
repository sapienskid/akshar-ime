/target
*.rs.bk
*.log
.vscode
.idea
.DS_Store
node_modules
dist
.env
*.swp
*.tmp
*.bak
.zcode/

# Generated transliteration model + downloaded Aksharantar corpus
data/nepali_model.bin
data/aksharantar/
data/translit_model.bin
data/roman_lexicon.bin
# WASM build output is committed for npm, but local dev may rebuild
# Uncomment to ignore if you prefer not to commit:
# wasm/pkg/
# js uses no build step — keep committed

# Private scraping pipeline + data (never tracked)
data-pipeline/
